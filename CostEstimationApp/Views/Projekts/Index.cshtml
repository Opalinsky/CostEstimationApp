@model IEnumerable<Projekt>

@{
    ViewData["Title"] = "Index";
}

<h1>Projekty</h1>

@* <p>
    <a asp-action="Create">Create New</a>

    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="SemiFinishedProducts" asp-action="Index">>>> Półfabrykaty >>></a>
    </li>
</p> *@
<p>
    <a class="btn btn-primary" asp-action="Create">Stwórz Nowy</a>
    <a class="btn btn-secondary" asp-controller="SemiFinishedProducts" asp-action="Index">Półfabrykaty >></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Name)*@
                Nazwa Projektu
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.Quantity) *@
                Ilość Sztuk
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.SemiFinishedProduct.) *@
                Półfabrykat
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.TotalCost) *@
                Koszt Materiału
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.TotalCost) *@
                Koszt Operacji
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SemiFinishedProduct.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SemiFinishedProductCost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalCost)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Szczegóły</a> |
                <a asp-action="Select" asp-route-id="@item.Id">Wybierz</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
