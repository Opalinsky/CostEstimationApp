@model IEnumerable<CostEstimationApp.Models.MachineType>

@{
    ViewData["Title"] = "Index";
}

<h1>Typy Maszyn</h1>

<p>
@*     <a asp-action="Create">Create New</a 
 *@</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.Typeof) *@
                Typ maszyny
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.AdditionalTime) *@
                Czas pomocniczny (tp)
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.AuxiliaryTime) *@
                Czas uzupełniający (tu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Typeof)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuxiliaryTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<a class="btn btn-secondary" asp-controller="Machines" asp-action="Index">Maszyny <<</a>

